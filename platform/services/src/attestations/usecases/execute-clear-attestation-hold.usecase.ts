/**
 * ExecuteClearAttestationHold - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: attestations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteClearAttestationHold Use Case
 *
 * DDD: Application use case for get ClearHold.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ClearAttestationHoldInput, ClearAttestationHoldOutput } from "../dto/clear-hold.dto";
import type { ExecutionContextService, IdGeneratorService } from "@peerwheel/services/_shared/index.js";
import type { ClearHoldRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteClearAttestationHold {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly clearHold: ClearHoldRepository
  ) {}

  async execute(input: ClearAttestationHoldInput): Promise<ClearAttestationHoldOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.attId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).attestationId) {
      throw new ValidationError("attestationId is required");
    }
    const attestationId = (input as any).attestationId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.clearHold.clearAttestationHold({ attestationId: attestationId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${attestationId}`);
    }

    return entity as ClearAttestationHoldOutput;
  }
}
