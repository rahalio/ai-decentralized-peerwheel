/**
 * Settlement Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: settlements
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { SettlementEntity } from "@peerwheel/core/settlements/models/index.js";
import { settlementsSchemas } from "@peerwheel/core/settlements";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "stl";

export function createSettlement(overrides: Partial<SettlementEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    amount: overrides.amount ?? faker.number.int(),
    currency: overrides.currency ?? faker.lorem.word(),
    platformFeeRate: overrides.platformFeeRate ?? faker.number.int(),
    rail: overrides.rail ?? "fiat",
    rideId: overrides.rideId ?? faker.string.uuid(),
    settlementId: overrides.settlementId ?? `stl_${ulid().toLowerCase()}`,
    status: overrides.status ?? "pending",
    ...overrides,
  };

  return settlementsSchemas.Settlement.parse(raw);
}

export function settlementBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createSettlement(overrides));
}
