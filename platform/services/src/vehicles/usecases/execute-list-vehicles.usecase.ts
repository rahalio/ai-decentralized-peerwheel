/**
 * ExecuteListVehicles - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: vehicles
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListVehicles Use Case
 *
 * DDD: Application use case for list Vehicle.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListVehiclesInput, ListVehiclesOutput } from "../dto/vehicle.dto";
import type { ExecutionContextService, IdGeneratorService } from "@peerwheel/services/_shared/index.js";
import type { VehicleRepository } from "../ports";

export class ExecuteListVehicles {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly vehicle: VehicleRepository
  ) {}

  async execute(input: ListVehiclesInput): Promise<ListVehiclesOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.vehId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.vehicle.listVehicles({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListVehiclesOutput;
  }
}
