/**
 * UpdateVehicle handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: vehicles
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updateVehicle } from "@peerwheel/api-server/domains/vehicles/handlers";
import type { VehiclesDDDDependencies } from "@peerwheel/api-server/domains/vehicles";
import { createVehicle as createVehicleEntity } from "@peerwheel/factories/vehicles";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@peerwheel/tests/shared";
import { VEHICLES_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdateVehicle", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: VehiclesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<VehiclesDDDDependencies>(
      VEHICLES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createVehicleEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { vehicleId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdateVehicle, "execute").mockResolvedValue(useCaseResult);

    await updateVehicle(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdateVehicle.execute).toHaveBeenCalledWith(expect.objectContaining({ vehicleId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
