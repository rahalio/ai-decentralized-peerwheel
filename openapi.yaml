# Peerwheel OpenAPI — live specs live under packages/openapi-core/src/
# One YAML file per domain:
#   identity.yaml, vehicles.yaml, rides.yaml, attestations.yaml,
#   settlements.yaml, disputes.yaml, canvas.yaml, corridors.yaml
# Bundle with: pnpm bundle:openapi
