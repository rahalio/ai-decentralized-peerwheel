/**
 * ExecuteUpdateCorridorLicensing - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: corridors
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteUpdateCorridorLicensing Use Case
 *
 * DDD: Application use case for update Licensing.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { UpdateCorridorLicensingInput, UpdateCorridorLicensingOutput } from "../dto/licensing.dto";
import type { ExecutionContextService, IdGeneratorService } from "@peerwheel/services/_shared/index.js";
import type { LicensingRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanUpdate } from "../policies/can-update.policy";
export class ExecuteUpdateCorridorLicensing {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly licensing: LicensingRepository
  ) {}

  async execute(input: UpdateCorridorLicensingInput): Promise<UpdateCorridorLicensingOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.corId();

    // All field names extracted from OpenAPI spec
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).corridorId) {
      throw new ValidationError("corridorId is required");
    }
    const corridorId = (input as any).corridorId;
    // No get/list operation for this resource - call update directly
    // Check policy
    if (!CanUpdate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Update entity via repository (pass correlation ID for response meta)
    const updated = await this.licensing.updateCorridorLicensing({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return updated as UpdateCorridorLicensingOutput;
  }
}
